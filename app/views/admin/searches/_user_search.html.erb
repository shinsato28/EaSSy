<!--ユーザー検索結果の部分テンプレーション-->
<!--検索結果がない場合文字を返す-->
<% if records.count == 0 %>
  <h4>ユーザーが見つかりませんでした。</h4>
<% else %>
  <% records.each do |user| %>
    <div class="card mb-1">
      <div class="card-body">
        <div class="card-title"><%= link_to user.name, user_path(user) %></div>
        <div class="card-text">
          投稿作品数: <%= link_to user.novels.count, novels_index_user_path(user) %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>