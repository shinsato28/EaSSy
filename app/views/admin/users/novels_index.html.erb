<div class="container mt-2 mb-5">
  <div class="row d-flex justify-content-center">
    <div class="col-8">
      <h3 class="mt-3"><%= @user.name %> 様の投稿</h3>
      <%= render 'novels_index', novels: @novels %>
    </div>
  </div>
</div>

<%= paginate @novels %>